[user]
	email = tkareine@gmail.com
	name = Tuomas Kareinen
[alias]
	authors = !sh -c 'git log --format=\"%an <%ae>\" ${1-master} -- | sort | uniq -c' -
	br  = branch
	ci  = commit -v
	co  = checkout
	cp  = cherry-pick
	df  = diff
	dfc = diff --cached
	fep = fetch --prune
	gr  = grep -n
	lg  = log --patch-with-stat --decorate
	lt  = log --graph --pretty=format:'%C(yellow)%h%Creset%C(yellow)%d%Creset %s %C(red)%ar%Creset by %C(green)%an%Creset'
	pl  = pull --ff-only
	rv  = revert
	st  = status
[color]
	ui = auto
[core]
	excludesfile = ~/.gitignore
[diff]
	tool = opendiff
[difftool]
	prompt = false
[format]
	numbered = auto
[merge]
	conflictstyle = diff3
[push]
	default = nothing
[rebase]
	autosquash = true
[rerere]
	enabled = true
