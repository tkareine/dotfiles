#!/bin/sh

# Usage:
#   echo input | well arg1 arg2
#
# Later, check ~/tmp/well.XXXXXX what where the args and stdin to the
# script.

basename=$(basename "$0")
out_file=$(mktemp ~/tmp/"${basename}".XXXXXX) || exit 1

echo "ARGS  : $*" > "$out_file"
echo "STDIN :" >> "$out_file"
cat >> "$out_file"
